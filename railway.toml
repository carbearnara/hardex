[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
watchPatterns = ["packages/oracle-service/**"]

[deploy]
healthcheckPath = "/health"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
